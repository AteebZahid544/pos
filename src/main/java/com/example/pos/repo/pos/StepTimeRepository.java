package com.example.pos.repo.pos;

import com.example.pos.entity.pos.StepTime;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StepTimeRepository extends JpaRepository<StepTime, Long> {}
